export enum ESubmissionStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum EFieldType {
  NUMBER = 'number',
  TEXT = 'text',
  DATE = 'date',
  UPLOAD = 'upload',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
}

export enum EFormTypeScope {
  ALL = 'all',
  PROBATION = 'probation',
  PERMANENT = 'permanent',
}
